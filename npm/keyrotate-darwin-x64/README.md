# keyrotate-darwin-x64

The darwin-x64 binary for [`keyrotate`](https://www.npmjs.com/package/keyrotate). Install the main `keyrotate` package instead — npm will pick this one for you automatically based on your platform.

MIT © BotFlow Lab
